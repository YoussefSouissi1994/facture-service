spring:
  application:
    name: facture-service
  cloud:
    config:
      fail-fast: true
      username: admin
      password: admin
      uri: http://localhost:8888

  kafka:
    bootstrap-servers: localhost:9092
    clientId: facture-service
